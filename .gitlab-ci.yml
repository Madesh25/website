pages:
  stage: deploy
  script:
    - apt-get update && apt-get install -y bash
    - bash -c 'shopt -s extglob && mkdir public && cp -r !(public) public/'
  artifacts:
    paths:
      - public
  only:
    - main